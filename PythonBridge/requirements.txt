# MFLUX Mac App Python Dependencies
# Updated with correct package names for automated installation

# Core PyTorch for Apple Silicon (required by MFLUX)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Core MFLUX for AI image generation on Apple Silicon
mflux>=0.9.6

# Core MLX (Apple Silicon optimized)
mlx>=0.19.0

# Image processing
Pillow>=10.0.0
numpy>=1.24.0

# Utilities
requests>=2.31.0
tqdm>=4.65.0

# Additional dependencies that MFLUX may need
transformers>=4.30.0
huggingface-hub>=0.16.0
safetensors>=0.3.0

# SentencePiece - required by T5Tokenizer and other transformer models
sentencepiece>=0.1.99

# Development and testing (optional)
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0

# Note: This will automatically install real MFLUX for AI image generation
# Compatible with Python 3.10, 3.11, 3.12, 3.13 on macOS 